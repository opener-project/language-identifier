#!/usr/bin/env ruby
$:.unshift(File.dirname(__FILE__) + '/../lib') unless $:.include?(File.dirname(__FILE__) + '/../lib')

require 'Vicom-language-identifier-lite_kernel'

if ARGV.size > 0
  puts `#{KERNEL_CORE} #{ARGV[0]} #{ARGV[1]}`
else
  puts `#{KERNEL_CORE}`
end


